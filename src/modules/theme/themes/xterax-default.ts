import type { Theme } from "../types";

export const xteraxDefault: Theme = {
  id: "xterax-default",
  name: "Xterax Default",
  description: "The default Xterax look — clean glass over neutral surfaces.",
  editorTheme: { dark: "atomone", light: "atomone" },
  variants: {
    light: {},
    dark: {},
  },
};
